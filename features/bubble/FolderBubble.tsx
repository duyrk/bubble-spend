// Folder bubble — groups multiple categories into a single expandable bubble
// Placeholder for future implementation

import { View } from 'react-native';

export function FolderBubble() {
  return <View />;
}
