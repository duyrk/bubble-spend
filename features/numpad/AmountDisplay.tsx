// Amount display — shows the number being entered in the numpad
// Placeholder for future implementation

import { View } from 'react-native';

export function AmountDisplay() {
  return <View />;
}
